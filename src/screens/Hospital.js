import React from 'react';
import {Text, View} from 'react-native';

const Hospital = ({navigation}) => {
  // console.log(55, props.navigation);
  return (
    <View>
      <Text> textInComponent </Text>
    </View>
  );
};

export default Hospital;
